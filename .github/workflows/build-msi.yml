name: Build MSI Package

on:
  push:
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      create_release:
        description: 'Create GitHub release (only works with manual trigger)'
        required: false
        type: boolean
        default: false
      version:
        description: 'Version number for release (e.g., 1.0.3.0)'
        required: false
        type: string
        default: '1.0.2.0'

env:
  SOLUTION_PATH: KAIROS.csproj
  INSTALLER_PATH: KAIROS.Installer\KAIROS.Installer.wixproj
  BUILD_CONFIGURATION: Release

jobs:
  build-msi:
    strategy:
      matrix:
        platform: [x64, x86]
    
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.3
    
    - name: Install WiX Toolset
      run: |
        Write-Host "Downloading WiX Toolset..."
        $wixUrl = "https://github.com/wixtoolset/wix3/releases/download/wix3112rtm/wix311.exe"
        $wixInstaller = "$env:TEMP\wix311.exe"
        Invoke-WebRequest -Uri $wixUrl -OutFile $wixInstaller
        
        Write-Host "Installing WiX Toolset..."
        Start-Process -FilePath $wixInstaller -ArgumentList "/quiet" -Wait
        
        # Add WiX to PATH
        $wixPath = "${env:ProgramFiles(x86)}\WiX Toolset v3.11\bin"
        echo "$wixPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
        
        Write-Host "WiX Toolset installed successfully"
      shell: powershell
    
    - name: Restore NuGet packages
      run: dotnet restore ${{ env.SOLUTION_PATH }}
    
    - name: Build and Package MSI
      run: |
        cd KAIROS.Installer
        .\Build-MSI.ps1 -Platform ${{ matrix.platform }} -Configuration ${{ env.BUILD_CONFIGURATION }}
      shell: powershell
    
    - name: Sign MSI (if certificate is available)
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
      env:
        CERT_BASE64: ${{ secrets.SIGNING_CERTIFICATE }}
        CERT_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
      run: |
        if ($env:CERT_BASE64) {
          Write-Host "Certificate found, signing MSI..."
          
          # Decode certificate from base64
          $certBytes = [System.Convert]::FromBase64String($env:CERT_BASE64)
          $certPath = "$env:TEMP\cert.pfx"
          [System.IO.File]::WriteAllBytes($certPath, $certBytes)
          
          # Find SignTool
          $signTool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" -Recurse | 
                      Sort-Object FullName -Descending | 
                      Select-Object -First 1
          
          if ($signTool) {
            $msiPath = "KAIROS.Installer\bin\${{ matrix.platform }}\${{ env.BUILD_CONFIGURATION }}\KaiROS-AI-Setup.msi"
            
            & $signTool.FullName sign `
              /f $certPath `
              /p $env:CERT_PASSWORD `
              /t http://timestamp.digicert.com `
              $msiPath
            
            Write-Host "MSI signed successfully"
          } else {
            Write-Warning "SignTool not found, skipping signing"
          }
          
          # Clean up certificate
          Remove-Item $certPath -Force
        } else {
          Write-Host "No certificate configured, skipping signing"
        }
      shell: powershell
    
    - name: Get Version
      id: version
      run: |
        $version = "1.0.2.0"
        if ($env:GITHUB_REF -match 'refs/tags/v(.+)') {
          $version = $matches[1]
        } elseif (-not [string]::IsNullOrEmpty("${{ github.event.inputs.version }}")) {
          $version = "${{ github.event.inputs.version }}"
        }
        echo "version=$version" >> $env:GITHUB_OUTPUT
        Write-Host "Building version: $version"
      shell: powershell
    
    - name: Upload MSI Artifact
      uses: actions/upload-artifact@v4
      with:
        name: KaiROS-AI-Setup-${{ matrix.platform }}-v${{ steps.version.outputs.version }}
        path: KAIROS.Installer\bin\${{ matrix.platform }}\${{ env.BUILD_CONFIGURATION }}\KaiROS-AI-Setup.msi
        retention-days: 30
    
    - name: Upload Validation Report
      uses: actions/upload-artifact@v4
      with:
        name: ValidationReport-${{ matrix.platform }}
        path: KAIROS.Installer\bin\${{ matrix.platform }}\${{ env.BUILD_CONFIGURATION }}\ValidationReport.txt
        retention-days: 30

  create-release:
    needs: build-msi
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true')
    runs-on: windows-latest
    
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get Version (create-release job)
      id: version
      run: |
        $version = $env:GITHUB_REF -replace 'refs/tags/v', ''
        if ($version -eq $env:GITHUB_REF) {
          # Not a tag, use workflow input
          $version = "${{ github.event.inputs.version }}"
        }
        echo "version=$version" >> $env:GITHUB_OUTPUT
        Write-Host "Release version: $version"
      shell: powershell
    
    - name: Download x64 MSI
      uses: actions/download-artifact@v4
      with:
        name: KaiROS-AI-Setup-x64-v${{ steps.version.outputs.version }}
        path: release/x64
    
    - name: Download x86 MSI
      uses: actions/download-artifact@v4
      with:
        name: KaiROS-AI-Setup-x86-v${{ steps.version.outputs.version }}
        path: release/x86
    
    - name: Download Validation Reports
      uses: actions/download-artifact@v4
      with:
        pattern: ValidationReport-*
        path: release/reports
    
    - name: Create Release Notes
      run: |
        $version = "${{ steps.version.outputs.version }}"
        $releaseNotes = @"
        # KaiROS AI v$version
        
        ## Installation
        
        Download the appropriate MSI installer for your system:
        - **x64 (64-bit)**: Recommended for most modern Windows systems
        - **x86 (32-bit)**: For older or 32-bit Windows systems
        
        ### Silent Installation
        ``````powershell
        msiexec /i KaiROS-AI-Setup.msi /quiet /qn
        ``````
        
        ### Requirements
        - Windows 10 version 1809 (Build 17763) or later
        - 4 GB RAM minimum (8 GB recommended)
        - 500 MB free disk space
        
        ## Changes
        
        See [CHANGELOG.md](CHANGELOG.md) for detailed changes.
        
        ## Support
        
        - Documentation: [README.md](README.md)
        - Issues: [GitHub Issues](https://github.com/avikeid2007/KaiROS/issues)
        "@
        
        $releaseNotes | Out-File -FilePath release/RELEASE_NOTES.md -Encoding UTF8
      shell: powershell
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release/x64/KaiROS-AI-Setup.msi
          release/x86/KaiROS-AI-Setup.msi
          release/reports/**/*.txt
        body_path: release/RELEASE_NOTES.md
        draft: false
        prerelease: false
        token: ${{ secrets.GITHUB_TOKEN }}

  # Optional: Store submission preparation
  prepare-store-submission:
    needs: build-msi
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true')
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get Version (prepare-store-submission job)
      id: version
      run: |
        $version = $env:GITHUB_REF -replace 'refs/tags/v', ''
        if ($version -eq $env:GITHUB_REF) {
          # Not a tag, use workflow input
          $version = "${{ github.event.inputs.version }}"
        }
        echo "version=$version" >> $env:GITHUB_OUTPUT
        Write-Host "Store submission version: $version"
      shell: powershell
    
    - name: Download x64 MSI
      uses: actions/download-artifact@v4
      with:
        name: KaiROS-AI-Setup-x64-v${{ steps.version.outputs.version }}
        path: store-submission
    
    - name: Download Validation Report
      uses: actions/download-artifact@v4
      with:
        name: ValidationReport-x64
        path: store-submission
    
    - name: Create Submission Package
      run: |
        $submissionPath = "store-submission"
        
        # Copy documentation
        Copy-Item "KAIROS.Installer\SUBMISSION-GUIDE.md" -Destination "$submissionPath\"
        Copy-Item "KAIROS.Installer\QUICK-REFERENCE.md" -Destination "$submissionPath\"
        Copy-Item "README.md" -Destination "$submissionPath\"
        
        # Create submission checklist
        $checklist = @"
        Microsoft Store Submission Checklist for v${{ steps.version.outputs.version }}
        
        Pre-Submission:
        [ ] Review validation report
        [ ] Test MSI on clean Windows 10 machine
        [ ] Run Windows App Certification Kit (WACK)
        [ ] Verify all features work correctly
        [ ] Update store listing description
        [ ] Prepare screenshots (1-10)
        [ ] Update privacy policy (if needed)
        
        Partner Center Steps:
        [ ] Log in to Partner Center
        [ ] Create new submission
        [ ] Upload MSI (x64)
        [ ] Complete store listing
        [ ] Set pricing and availability
        [ ] Submit for certification
        
        Files in this package:
        - KaiROS-AI-Setup.msi: Main installer package
        - ValidationReport.txt: Automated validation results
        - SUBMISSION-GUIDE.md: Detailed submission guide
        - QUICK-REFERENCE.md: Quick command reference
        - README.md: Project documentation
        "@
        
        $checklist | Out-File -FilePath "$submissionPath\SUBMISSION-CHECKLIST.txt" -Encoding UTF8
        
        # Create ZIP archive
        Compress-Archive -Path "$submissionPath\*" -DestinationPath "KaiROS-Store-Submission-v${{ steps.version.outputs.version }}.zip"
      shell: powershell
    
    - name: Upload Store Submission Package
      uses: actions/upload-artifact@v4
      with:
        name: Microsoft-Store-Submission-v${{ steps.version.outputs.version }}
        path: KaiROS-Store-Submission-v${{ steps.version.outputs.version }}.zip
        retention-days: 90

  # Deploy releases to GitHub Pages
  deploy-to-pages:
    needs: [build-msi, create-release, prepare-store-submission]
    if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.create_release == 'true')
    runs-on: windows-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get Version (deploy-to-pages job)
      id: version
      run: |
        $version = $env:GITHUB_REF -replace 'refs/tags/v', ''
        if ($version -eq $env:GITHUB_REF) {
          # Not a tag, use workflow input
          $version = "${{ github.event.inputs.version }}"
        }
        echo "version=$version" >> $env:GITHUB_OUTPUT
        Write-Host "Deploying version: $version"
      shell: powershell
    
    - name: Download x64 MSI
      uses: actions/download-artifact@v4
      with:
        name: KaiROS-AI-Setup-x64-v${{ steps.version.outputs.version }}
        path: downloads/msi/x64
    
    - name: Download x86 MSI
      uses: actions/download-artifact@v4
      with:
        name: KaiROS-AI-Setup-x86-v${{ steps.version.outputs.version }}
        path: downloads/msi/x86
    
    - name: Download Store Submission Package
      uses: actions/download-artifact@v4
      with:
        name: Microsoft-Store-Submission-v${{ steps.version.outputs.version }}
        path: downloads/store
    
    - name: Create Downloads Page
      run: |
        $version = "${{ steps.version.outputs.version }}"
        
        # Create downloads directory structure
        New-Item -ItemType Directory -Force -Path "pages/downloads/$version"
        
        # Copy MSI files
        Copy-Item "downloads/msi/x64/KaiROS-AI-Setup.msi" -Destination "pages/downloads/$version/KaiROS-AI-Setup-x64-v$version.msi"
        Copy-Item "downloads/msi/x86/KaiROS-AI-Setup.msi" -Destination "pages/downloads/$version/KaiROS-AI-Setup-x86-v$version.msi"
        Copy-Item "downloads/store/KaiROS-Store-Submission-v$version.zip" -Destination "pages/downloads/$version/"
        
        # Create version-specific download page
        $versionPage = @"
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>KaiROS AI v$version - Downloads</title>
            <style>
                body {
                    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
                    max-width: 1200px;
                    margin: 0 auto;
                    padding: 20px;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: #fff;
                }
                .container {
                    background: rgba(255, 255, 255, 0.1);
                    backdrop-filter: blur(10px);
                    border-radius: 20px;
                    padding: 40px;
                    box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
                }
                h1 {
                    font-size: 2.5em;
                    margin-bottom: 10px;
                }
                .version {
                    font-size: 1.2em;
                    opacity: 0.9;
                    margin-bottom: 30px;
                }
                .download-section {
                    background: rgba(255, 255, 255, 0.2);
                    border-radius: 15px;
                    padding: 30px;
                    margin: 20px 0;
                }
                .download-btn {
                    display: inline-block;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    padding: 15px 30px;
                    margin: 10px 10px 10px 0;
                    border-radius: 10px;
                    text-decoration: none;
                    font-weight: bold;
                    transition: transform 0.2s;
                    box-shadow: 0 4px 15px 0 rgba(0, 0, 0, 0.2);
                }
                .download-btn:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 6px 20px 0 rgba(0, 0, 0, 0.3);
                }
                .file-info {
                    font-size: 0.9em;
                    opacity: 0.8;
                    margin-top: 5px;
                }
                .recommended {
                    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
                }
                footer {
                    text-align: center;
                    margin-top: 40px;
                    opacity: 0.8;
                }
                .back-link {
                    color: #fff;
                    text-decoration: none;
                    opacity: 0.8;
                }
                .back-link:hover {
                    opacity: 1;
                }
            </style>
        </head>
        <body>
            <div class="container">
                <h1>🤖 KaiROS AI</h1>
                <div class="version">Version $version</div>
                
                <div class="download-section">
                    <h2>📦 MSI Installers (Recommended)</h2>
                    <p>Professional installers for Windows 10/11. Includes silent install support.</p>
                    
                    <a href="KaiROS-AI-Setup-x64-v$version.msi" class="download-btn recommended">
                        ⬇️ Download x64 (64-bit)
                    </a>
                    <div class="file-info">Recommended for most modern Windows systems</div>
                    
                    <br><br>
                    
                    <a href="KaiROS-AI-Setup-x86-v$version.msi" class="download-btn">
                        ⬇️ Download x86 (32-bit)
                    </a>
                    <div class="file-info">For older or 32-bit Windows systems</div>
                    
                    <h3>Installation</h3>
                    <pre style="background: rgba(0,0,0,0.3); padding: 15px; border-radius: 5px; overflow-x: auto;">
        # Interactive install (double-click the downloaded MSI file)
        # Or silent install (replace filename with downloaded file):
        msiexec /i KaiROS-AI-Setup-x64-v$version.msi /quiet /qn</pre>
                </div>
                
                <div class="download-section">
                    <h2>🏪 Microsoft Store Submission Package</h2>
                    <p>Complete package for Microsoft Store submission with documentation and validation reports.</p>
                    
                    <a href="KaiROS-Store-Submission-v$version.zip" class="download-btn">
                        ⬇️ Download Store Submission Package
                    </a>
                    <div class="file-info">Includes MSI, documentation, and submission checklist</div>
                </div>
                
                <div class="download-section">
                    <h2>📋 Requirements</h2>
                    <ul>
                        <li>Windows 10 version 1809 (Build 17763) or later</li>
                        <li>4 GB RAM minimum (8 GB recommended)</li>
                        <li>500 MB free disk space</li>
                        <li>Internet connection for initial model download</li>
                    </ul>
                </div>
                
                <footer>
                    <p><a href="../../index.html" class="back-link">← Back to All Releases</a></p>
                    <p><a href="https://github.com/avikeid2007/KaiROS" class="back-link">View on GitHub</a></p>
                </footer>
            </div>
        </body>
        </html>
        "@
        
        $versionPage | Out-File -FilePath "pages/downloads/$version/index.html" -Encoding UTF8
        
        # Update or create main downloads index
        $mainPage = @"
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>KaiROS AI - Downloads</title>
            <style>
                body {
                    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
                    max-width: 1200px;
                    margin: 0 auto;
                    padding: 20px;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: #fff;
                    min-height: 100vh;
                }
                .container {
                    background: rgba(255, 255, 255, 0.1);
                    backdrop-filter: blur(10px);
                    border-radius: 20px;
                    padding: 40px;
                    box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
                }
                h1 {
                    font-size: 3em;
                    margin-bottom: 10px;
                    text-align: center;
                }
                .tagline {
                    text-align: center;
                    font-size: 1.3em;
                    opacity: 0.9;
                    margin-bottom: 50px;
                }
                .release-card {
                    background: rgba(255, 255, 255, 0.2);
                    border-radius: 15px;
                    padding: 30px;
                    margin: 20px 0;
                    transition: transform 0.2s;
                }
                .release-card:hover {
                    transform: translateY(-5px);
                    box-shadow: 0 8px 25px 0 rgba(0, 0, 0, 0.3);
                }
                .release-version {
                    font-size: 1.8em;
                    font-weight: bold;
                    margin-bottom: 10px;
                }
                .release-date {
                    opacity: 0.8;
                    margin-bottom: 20px;
                }
                .download-link {
                    display: inline-block;
                    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                    color: white;
                    padding: 15px 30px;
                    border-radius: 10px;
                    text-decoration: none;
                    font-weight: bold;
                    transition: transform 0.2s;
                    box-shadow: 0 4px 15px 0 rgba(0, 0, 0, 0.2);
                }
                .download-link:hover {
                    transform: translateY(-2px);
                    box-shadow: 0 6px 20px 0 rgba(0, 0, 0, 0.3);
                }
                .latest-badge {
                    display: inline-block;
                    background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
                    padding: 5px 15px;
                    border-radius: 20px;
                    font-size: 0.8em;
                    margin-left: 10px;
                }
                footer {
                    text-align: center;
                    margin-top: 50px;
                    opacity: 0.8;
                }
                .github-link {
                    color: #fff;
                    text-decoration: none;
                }
                .github-link:hover {
                    text-decoration: underline;
                }
            </style>
        </head>
        <body>
            <div class="container">
                <h1>🤖 KaiROS AI</h1>
                <div class="tagline">Intelligence, Precisely When It Matters.</div>
                
                <div class="release-card">
                    <div class="release-version">
                        Version $version
                        <span class="latest-badge">LATEST</span>
                    </div>
                    <div class="release-date">Released: $(Get-Date -Format "MMMM dd, yyyy")</div>
                    <p>Professional AI chat assistant with local LLM support.</p>
                    <a href="$version/index.html" class="download-link">⬇️ Download v$version</a>
                </div>
                
                <footer>
                    <p><a href="https://github.com/avikeid2007/KaiROS" class="github-link">🔗 View Project on GitHub</a></p>
                    <p><a href="https://github.com/avikeid2007/KaiROS/blob/main/README.md" class="github-link">📖 Documentation</a></p>
                </footer>
            </div>
        </body>
        </html>
        "@
        
        $mainPage | Out-File -FilePath "pages/downloads/index.html" -Encoding UTF8
        
        # Create root index redirecting to downloads
        $rootPage = @"
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta http-equiv="refresh" content="0; url=downloads/index.html">
            <title>KaiROS AI - Redirecting...</title>
        </head>
        <body>
            <p>Redirecting to <a href="downloads/index.html">downloads page</a>...</p>
        </body>
        </html>
        "@
        
        $rootPage | Out-File -FilePath "pages/index.html" -Encoding UTF8
        
        Write-Host "Created GitHub Pages content for version $version"
      shell: powershell
    
    - name: Upload Pages Artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: pages
    
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
